{
  "name": "@vedin-pp-se/bolido-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^8.2.2"
  },
  "scripts": {
    "build": "lerna run build --stream",
    "link-all": "lerna exec npm link"
  }
}
